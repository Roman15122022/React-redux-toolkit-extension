export enum Language {
  EN = 'en',
  UA = 'ua',
}

export enum ThemeVariants {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum TypeButton {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
}
